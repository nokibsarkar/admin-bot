# admin-bot (<code>প্রশাসক বট</code>)
A bot with administrative power which do some automatic tasks that cannot be done without the power.
# Tasks
- Dive into recentchanges
 - Fetch the revisions with offensive words
  - Block the user
  - Hide the revision
